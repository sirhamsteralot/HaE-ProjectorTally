/* HaE Projectortally
 * Required blocks:
 * Assembler named "MasterAssembler" 
 * Projector named "TallyProjector"
 * 
 * After setup just load a blueprint and run the pb with "Start" argument.
 * this will get a list of components needed in the customdata section of the programmable block.
 * 
 * After that you can run the programmable block with the "Queue" argument.
 * This will queue the list of components in the Master Assembler.
 * 
 * Or run it with QStart and it will do both.
 */